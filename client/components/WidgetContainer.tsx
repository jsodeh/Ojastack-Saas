import React from "react";

// Minimal widget component that doesn't cause React errors
export default function WidgetContainer() {
  // Skip widget loading for now to prevent DOM conflicts
  // The widget can be re-enabled once environment is stable
  return null;
}
